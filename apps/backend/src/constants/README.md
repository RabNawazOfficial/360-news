# Constants Directory

This directory contains global, read-only constant values used across the codebase.

## Responsibility
Centralize standard codes, text prompts, error messages, or settings to avoid hardcoding strings and magic numbers throughout the codebase.

## Key Files
- `index.ts`: Contains and exports key application constants (e.g. HTTP status codes).
