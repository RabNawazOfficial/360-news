# Interfaces Directory

This directory houses TypeScript interface declarations that define contracts across different modules.

## Responsibility
It provides type definitions and contracts for structural reuse, helping decoupled layers (e.g. controllers, services, and models) communicate in a type-safe manner.

## Key Files
- `api.interface.ts`: Standard response formats, paging parameters, and standard API errors.
