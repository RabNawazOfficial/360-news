# Types Directory

This directory contains custom TypeScript type declarations and type aliases.

## Responsibility
Define custom models, aliases, union types, or extend third-party types (e.g. adding custom fields like `user` or metadata to Express `Request` object).

## Key Files
- `index.ts`: Standard shared types.
